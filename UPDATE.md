AI安全与敏感词替换方案讨论
2026-09-21 | 15:04

本次会议讨论了隐私保护的敏感词替换方案，明确了分层处理机制、管理后台功能，并探讨了后续的优化方向。

小结
1. 敏感词替换方案
整体方案分为两级：第一级为通用的mem privacy，用于替换密码、Token、用户名等固定敏感信息；第二级为可定制的敏感词库，用于处理业务相关的敏感内容。
敏感词库支持AI生成，并提供管理后台进行维护，包含敏感词、替换占位符和处理方式（替换/路由）三列。

2. 路由与上下文处理
当请求被识别为敏感时，会进行路由处理。对于上下文窗口大小不一致的内外网模型，需要通过agent联动来压缩上下文，确保安全。
讨论了将敏感词替换视为“远端专家模式”的可能性，即小模型优先处理，仅在必要时请求大模型，并由小模型自行完成脱敏。

3. 后续优化方向
为平衡安全与效率，后续将引入benchmark（基准测试）来评估不同方案的语义损失度。
基于benchmark结果，可进一步探索更优的处理策略，如小模型到大模型的专家模式。

待办
1. 敏感词替换方案落地
发言人1 负责完成第一期方案，包括实现通用的mem privacy能力以及可定制的敏感词词表管理后台。
发言人1 需支持通过内部大模型生成敏感词表，并提供管理后台进行维护。


============

大模型安全与商业模式探讨
2026-09-2115:21

本次会议主要讨论了AI应用的敏感词处理方案，明确了分级策略、专家模式架构，并探讨了通过benchmark进行效果评估的必要性，同时提出了与Kodax等项目的结合方式。

小结

1. 敏感词处理方案

当前方案仅支持替换和路由两种策略，需进行升级。
计划引入多级敏感词体系，并为每个敏感词设置级别。
同样需要为接入的大模型（Provider）和具体模型（Model）设置敏感级别，以实现更精细化的路由和透传控制。
未来可考虑将用户级别、数据权限与敏感词处理策略进行联动，实现更全面的权限管控。

2. Benchmark（基准测试）

需要建立两种类型的基准测试：
性能损失测试：对比脱敏前后的处理能力，评估不同脱敏方法对性能的影响。
专家模式测试：在小模型向大模型（专家模式）请求时，评估敏感信息的透传程度，确保其符合预期。

3. 产品价值与商业模式

该产品核心价值在于提供一个安全的AI应用层，确保用户隐私数据不出网关，不被大模型获取。
此方案可解决客户对私有化部署模型的顾虑，为大模型公司提供一个可信的商业化入口，建立新的商业模式。

4. 与Kodax等项目的结合

计划通过私有协议将Fabric与Kodax等项目结合，实现更强大的安全管控和数据溯源。
该结合方式可打通敏感信息识别、权限限制等信息，为Kodax提供更安全的私有化服务。
待办

1. 敏感词处理方案调研

负责调研其他可用的模型。
负责调研其他可用的模型。
负责调研在场景中是否有其他模式需要考虑。

2. 产品价值与商业模式

发言人1 需要思考如何将该产品的商业价值，特别是其商业模式，更好地呈现给老板。

============

本次会议围绕ADE平台的架构设计、核心流程、数据与本体关系、场景应用及安全成本统计等议题进行了深入讨论，明确了后续的设计方向和关键节点。

小结

1. ADE平台架构与核心流程

部署阶段：平台管理员与实施人员进行私有化部署，包括部署ADE服务、ADE Adapter（供Cotus调用的Extension）和进行身份对接。身份对接可采用环境变量或Extension内配置文件中的Key进行。
数据接入与语义资产初始化：通过安装行业包（包含通用对象类型和关系定义）来初始化基础本体。数据接入阶段会配置数据源、能力提供器（描述数据源的操作能力）和数据绑定（将业务描述与具体字段、计算方式关联）。
使用与发布：经过第一轮验证和发布后，系统会进入实际使用阶段，通过用户常用问题的自动化套件进行验证和校准，最终将资产发布供用户使用。

2. 核心技术问题与设计优化

图谱与数据存储：会议明确，原始数据不应全部存入图数据库，应与图谱（本体、关系）进行解耦。原始数据应通过缓存等方式在查询时获取，以保证灵活性和效率。
提示词骨架与结构化转换：当前通过提示词将自然语言问题转换为结构化查询的方案效果不稳定。建议探索使用Tool Code模式或提供多套工具（如选模板、填词）的Extension方案，以提升转换的准确性和可控性。
本体与流程的融合：当前设计中，流程与本体存在割裂。后续设计需将流程（如查询链路）与本体（实体、关系）进行深度融合，例如将查询链路作为图谱中的一个节点或流程图，以提升系统的可解释性和复杂任务的处理能力。
MCP与Skill管理：提出ADE应作为AI Agent的上下文构建器，管理MCP（工具调用）和Skill（能力提供）。这可以解决上层Agent管理海量外部工具的复杂性问题，使上下层系统解耦。

3. 场景设计与价值定位

推荐应用场景：建议优先选择金融投研类场景进行产品设计和演示。该场景数据公开、信息丰富、问题复杂，能充分展现ADE在构建复杂知识图谱和处理多步骤查询上的价值，便于找到早期客户。
产品价值：明确ADE的核心价值在于为AI Agent提供一个结构化的、可追溯的上下文（Context），不仅用于查询类问题的证据链构建，也用于动作类问题的执行链路规划。

4. 安全与成本统计

安全功能：明确需要在11月中旬前完成敏感词管理、敏感词检测和拦截功能的开发。
成本统计：需要对模型调用进行精细化的成本统计，按“读”、“写”、“缓存”等维度分类，并以图表形式清晰展示，以便进行后续的安全分析和成本控制。
待办

1. ADE平台设计与开发

负责主攻场景设计、架构设计和Demo准备。
负责跟进数据接入、能力绑定等流程的设计。
和 需在节后一周内，将场景和Demo描述定型。
和 需在11月中旬前，完成敏感词管理、检测和拦截功能的开发。

2. 技术方案与模型调研

需研究并尝试使用Tool Code模式或提供多套工具的Extension方案，以优化提示词骨架的结构化转换效果。
需调研并评估使用Jev模型进行本体构建的可行性与风险。
需研究如何将查询流程与本体图谱进行深度融合的设计方案。

3. 成本统计功能开发

需完成模型调用成本的精细化统计功能，按“读”、“写”、“缓存”等维度分类，并以图表形式展示。


=========

**是的。对于你现在列出的这些任务，前面那套架构作为“隐私网关”有价值，但作为完整的企业 AI 工作架构，确实不够。**

前面已经提到了本地计算、混合路由和工具授权，但还没有把它们落实成能够支撑**代码理解、业务计算、图推理和优化执行**的完整机制。

需要改进的核心，不是再训练一个“更会识别敏感词的模型”，而是把架构从：

> **请求级脱敏：识别敏感内容 → 替换 → 云端生成 → 本地恢复**

升级为：

> **任务级数据使用治理：判断任务需要什么信息 → 决定哪些可以披露、哪些必须本地处理 → 编译可执行计划 → 分步授权与执行 → 验证结果及信息流。**

而且，**这不应全部堆进 Fabric**。按照你之前确定的边界，应当由 **Agent DataEngine、KodaX Runtime、KodaX Fabric** 分工完成。

下面具体拆解。后文的架构、接口和执行方式是我的设计建议，不是 MemPrivacy 已经具备的功能。

---

# 一、为什么这四类任务不能用同一种“脱敏”解决

## 1.1 问题从“隐藏身份”变成了“控制业务语义的使用”

| 任务 | 模型真正需要理解的内容 | 可能需要保护的内容 | 只替换敏感值的问题 |
|---|---|---|---|
| 企业代码任务 | 接口、控制流、数据流、状态、异常、依赖关系 | 算法实现、业务规则、架构、凭据、客户样本 | 改名字不等于隐藏算法；删逻辑又可能无法修复 |
| 业务数据问答 | 指标定义、维度、关联条件、时间范围、真实数值 | 经营数据、客户明细、定价与成本 | 数值全部变成占位符后，无法准确计算与归因 |
| 业务优化 | 目标函数、决策变量、约束、预测与场景 | 成本参数、产能、合同约束、策略 | 参数隐藏后不能直接求解；约束本身也可能敏感 |
| 本体、图和知识库分析 | 对象类型、关系、路径、证据、规则及其时效 | 关系网络、供应链依赖、推导事实、内部知识 | 节点匿名了，关系和拓扑仍可能泄露核心信息 |

因此，你要保护的已经不只是“原始字符串”，而是四个层次：

**值、结构、逻辑、推导结果。**

例如：

```text
<供应商A> 是 <零件B> 的唯一来源；
<零件B> 被 <产线C> 和 <产线D> 共同依赖。
```

虽然名称全部被替换，但“存在共同单点供应风险”这个事实仍然暴露了。它可能正是企业最不希望外传的内容。

反过来，如果把“唯一来源”和“共同依赖”也删除，云端模型又无法分析供应风险。

**这不是改进占位符设计就能消除的矛盾。**

## 1.2 必须先承认一个架构边界

在普通明文模型 API 和占位符处理方式下，如果两个原始问题需要不同答案，却被转换成完全相同的输入，云端就没有足够信息区分它们。

因此，系统只能选择三类办法：

| 方式 | 外部模型能看到什么 | 谁完成敏感部分 |
|---|---|---|
| **计划外发、数据本地执行** | 获准公开的任务描述、接口和计算语义 | 本地数据库、图引擎、求解器或模型 |
| **最小必要语义披露** | 经批准的代码片段、子图、聚合结果或业务事实 | 云端进行真正的分析，本地继续控制执行 |
| **完整任务进入可信环境** | 未获准披露的内容不进入普通外部 API | 企业内部或明确获批的可信推理环境 |

这里不能走向另一个极端：

> “所有关键工作都交给本地小模型，云端只负责润色。”

那样可能失去调用顶级模型的主要价值。

**正确目标是：让强模型参与真正有价值的推理，但只参与它被允许获得足够信息的那一部分。**

---

# 二、企业代码任务：需要“代码上下文编译”，不是文本脱敏

## 2.1 代码里至少有三类不同的秘密

**第一类是外部附着信息。**  
例如 API Key、数据库密码、真实客户样本、内部域名。这些通常不是理解算法必需的内容，可以删除、替换，或者由可信执行层在运行时注入。

**第二类是业务身份和命名。**  
例如客户名称、项目代号、内部服务名。部分场景可以重命名，但必须尊重语言作用域、符号引用和运行机制，不能全局替换字符串。

**第三类是实现本身。**  
例如核心定价算法、风控规则、专有调度逻辑。它们即使改成 `function_a()`、`variable_b`，仍然可能完整暴露。

所以，代码任务的核心判断应当是：

> **这次修复必须理解哪些实现？这些实现是否允许披露？不能披露的部分，能否被一个经过验证的接口契约代替？**

## 2.2 建设本地的代码上下文编译能力

建议由 KodaX 的代码能力扩展，在客户环境中生成本次任务所需的上下文，而不是把整个仓库直接交给外部模型。

输入包括：

```text
任务描述
仓库版本
允许修改的范围
代码与文档权限
相关测试
数据出域政策
```

输出不是简单的“脱敏代码”，而是一个**任务相关的代码上下文包**：

| 内容 | 作用 |
|---|---|
| 相关函数与必要依赖 | 提供足够的真实代码语义 |
| 接口契约 | 描述输入、输出、异常、副作用和前置条件 |
| 状态与事务约束 | 避免只看局部代码得出错误结论 |
| 合成测试样本 | 替代真实客户或生产数据 |
| 必要的调用／数据流关系 | 帮助定位跨函数、跨模块问题 |
| 未披露依赖清单 | 告诉模型有哪些关键上下文未知，防止假装完整理解 |
| 修改边界 | 限制允许修改的文件、符号或模块 |

这类能力可以利用 AST、语言服务、调用关系和数据流分析，但不能认为“有 AST 就完整理解了程序”。CodeQL 官方文档明确区分了语法树与数据流图，也指出动态行为、别名、库代码和跨函数分析会带来完整性与计算成本问题。:chatgpt-content-reference{index="0"}

**所以我建议的是“结构分析＋测试＋任务相关检索”，而不是把代码图当作万能的隐私转换器。**

## 2.3 一个具体例子：修复库存超卖

假设任务是：

> 修复并发下库存扣减导致超卖的问题。

本地发现了一个类似这样的逻辑：

```python
stock = read_stock(sku)
if stock >= quantity:
    write_stock(sku, stock - quantity)
```

这段是说明原理的示例，不是你的实际代码。

对于云端分析，可能不需要披露客户名称、真实商品、生产连接串和完整订单数据；但它很可能需要知道：

**读取和写入是否在同一事务中、数据库隔离级别是什么、是否跨服务、是否要求幂等、库存是否还有其他扣减入口。**

因此，合理流程是：

```text
本地定位问题及相关依赖
→ 形成获准披露的最小复现与接口契约
→ 云端提出修复与测试方案
→ 本地检查修改边界并应用补丁
→ 在真实代码环境中编译和测试
→ 只反馈获准披露的失败证据
→ 继续修复
```

**“最小复现”不是随意删代码，而是保留产生问题所需的语义。**

如果并发错误来自一个不能披露的核心实现，就应该把对应分析留在可信环境，而不是给云端一个失真的样例，再指望答案准确。

## 2.4 补丁不能靠“反向字符串替换”恢复

建议补丁至少绑定：

**基准提交、文件或符号范围、修改前校验信息、允许的变更类型。**

如果用了符号别名，还需要结构化的源码映射和重新解析。补丁落地之后，要在真实代码上执行编译、类型检查、测试和必要的安全检查。

尤其要避免：

```text
云端修改脱敏仓库
→ 把所有占位符换回原名
→ 直接合并生产分支
```

这无法保证引用、反射、序列化字段、配置键和外部接口仍然正确。

此外，**云端生成代码在本地运行，不等于结果天然安全**。它可能读取不必要的文件，或者把敏感值编码进日志和测试输出。因此，本地执行仍然需要权限、网络、文件和输出限制。

---

# 三、业务数据问答：需要“语义查询＋本地精确执行”

## 3.1 不要把数据库问答理解成“把表格脱敏后发给模型”

以这个问题为例：

> 上个月毛利率为什么下降？主要是价格、成本还是产品结构变化？

模型真正需要解决的是：

**毛利率采用什么口径；月份是否已经结账；退货如何处理；维度如何关联；如何做变化分解；哪些结果具有解释意义。**

这里最需要的不是先读取每一条交易明细，而是获得正确的**业务语义和查询能力**。

我建议采用：

```text
自然语言问题
→ 解析业务意图
→ 绑定企业指标、维度和时间口径
→ 生成受限查询／分析计划
→ 本地执行
→ 对结果做披露决策
→ 云端或本地解释
```

## 3.2 对外暴露“业务能力”，而不是任意 SQL

例如，模型可以请求一个这样的能力。以下是接口意图示例，不是某个现有 API：

```text
metric.compare(
    metric="gross_margin_rate:v3",
    period="last_closed_month",
    baseline="previous_closed_month",
    dimensions=["product_family"],
    execution="local_exact"
)
```

模型不需要直接知道底层是三张表、一个数据仓库视图，还是跨系统查询。

本地语义层负责：

**指标定义、关联规则、数据权限、币种与单位、时间范围、缺失值处理、数据版本。**

但这些能力描述本身也要过权限检查。某个特殊指标的定义、某张表的名称，都可能透露企业战略，不能默认“schema 永远可以公开”。

## 3.3 权限要区分“看明细”和“允许计算”

这里有一个重要改进：

> **没有逐行读取权限的人，可能被允许获得某种经过批准的聚合结果。**

例如，某业务主管可以知道部门平均处理时长，但不能查看每个员工的全部工作记录。

因此，不能只有简单的“有数据权限／无数据权限”。应当支持：

**允许读取什么、允许计算什么、允许输出什么。**

可信服务可以在受约束的任务授权下执行聚合，但不能把服务自己的广泛权限继承给 Agent。

数据库自身的权限也必须实际生效。例如，PostgreSQL 的超级用户、`BYPASSRLS` 角色会绕过行级安全，表所有者通常也会绕过；不能一边使用高权限服务账号，一边以为定义了 RLS 就已经完成隔离。:chatgpt-content-reference{index="1"}

## 3.4 结果可以有三种去向

**精确数值只允许用户看。**  
本地生成准确表格、图表和结论；外部模型可以参与分析方法设计，但不读取这些数值。

**聚合结果允许交给获批模型。**  
把经批准的变化分解、区间、趋势或完整聚合值交给强模型，让它做更深入的解释和下一步分析。

**业务事实本身禁止外发。**  
连“毛利率下降”“某产品亏损”都不能外发时，相关解释必须留在可信环境。

这意味着，**云端得到的不应固定为占位符文本，而应当是按用途批准的结果视图。**

还要区分“数值分解”和“因果结论”：能够把变化分解为价格、成本、结构项，不等于已经证明某项管理动作造成了这些变化。系统应分别标注计算结果、假设和待验证解释。

## 3.5 不能只检查单次查询

即使每次都只输出聚合，连续查询也可能拼出敏感信息。

例如先问“全部员工的工资总额”，再问“除某人之外的工资总额”，相减即可获得该人的数值。这是从查询组合直接产生的风险。

因此，除了单次结果检查，还需要控制：

**可用维度、可用过滤条件、最小群组、反复缩小范围、跨查询组合和累计披露。**

Snowflake 的聚合政策就是一个现有工程参考：它能约束查询必须聚合，并要求达到最小群组规模；但这样的单类控制不应被你当作完整推断防护。:chatgpt-content-reference{index="2"}

---

# 四、业务优化：需要“建模、求解、仿真和执行”，不是让 LLM 直接给最优方案

## 4.1 先把“优化”拆成不同工作

以仓储或生产调度为例，一次真正的优化通常包括：

| 工作 | 适合承担的组件 |
|---|---|
| 理解业务目标、提出建模候选 | LLM＋业务人员 |
| 绑定指标、对象和约束 | DataEngine 语义层与业务规则 |
| 预测需求、工时或风险 | 对应预测模型 |
| 在约束下搜索可行方案 | 优化求解器或专用算法 |
| 检验方案在实际场景中的效果 | 仿真、回测或业务验证器 |
| 修改生产业务状态 | Runtime 调用获批业务能力 |

不是所有“业务优化”都适合数学求解器，但只要任务包含清晰的资源、约束和目标，就应评估这种拆分，而不是要求大模型凭语言生成一个“最优结果”。

## 4.2 把公式与私有参数分离

例如仓储任务可以采用一个候选目标：

\[
\min_x \;
C_{\text{搬运}}(x)
+\lambda C_{\text{延期}}(x)
+\mu C_{\text{加班}}(x)
\]

其中 \(x\) 是任务分配或调度方案。

外部模型可以帮助设计目标、约束组织方式和求解策略，但真实的订单、设备能力、成本参数、客户优先级和合同限制，可以在本地绑定与求解。

需要注意两点：

**权重和硬约束不能由模型随意决定。**  
“为了降成本可以延迟哪些订单”是业务政策，不是模型自由裁量。

**公式和约束也可能敏感。**  
某项客户优先策略即使没有真实客户名称，仍可能属于不能外发的商业规则。

## 4.3 云端可以参与迭代，但反馈也要受控

合理闭环是：

```text
云端提出建模／搜索方案
→ 本地绑定数据并求解
→ 本地验证约束
→ 返回允许披露的诊断
→ 云端调整建模或策略
→ 本地再次验证
```

反馈可以是“某类资源约束成为瓶颈”，也可以是获准公开的目标值和对比结果；不能默认把全部求解日志、约束参数和冲突集合返回云端。

同时，要准确区分：

**找到可行方案、找到当前最好方案、证明最优。**

例如 OR-Tools 的 CP-SAT 明确区分 `FEASIBLE` 与 `OPTIMAL`；前者不能被包装成已经证明最优。:chatgpt-content-reference{index="3"}

## 4.4 优化结果与实际业务动作必须分开授权

“建议调整调度”与“真的修改 WMS 或生产排程”是两种权限。

建议维持：

```text
候选方案
→ 约束检查
→ 仿真／回测
→ 业务批准或已定义的自动授权
→ 执行前重新检查数据版本
→ 幂等执行
→ 监测与必要补偿
```

不是所有业务动作都能回滚，因此不能把“有回滚按钮”当作统一安全保证。

这部分继续由 **KodaX Runtime 和既有 Capability Provider／Extension** 承担，不必另造一个与 Runtime 平行的 Action Gateway。

---

# 五、本体、图和知识库：需要治理“关系与推导”，而不只是字段

这是你这套平台最值得做深的地方。

## 5.1 先把几个概念分清

| 层次 | 回答的问题 |
|---|---|
| **本体与语义模型** | 有哪些业务对象、属性、关系、指标、规则和动作？ |
| **实体与事实图** | 当前具体有哪些对象，它们之间存在什么关系？ |
| **知识证据层** | 某个事实来自哪个文档、系统、时间和证据？ |
| **分析上下文** | 为当前任务，究竟选取了哪些对象、事实和证据？ |

这四层不能混成一个“大图”。

Palantir 的官方定义也不只是节点与边：其 Ontology 同时包含对象、属性、关系等语义元素，以及动作、函数和动态安全等操作元素。:chatgpt-content-reference{index="4"}

但对你的体系，**不需要把全部数据搬进图数据库**。本体可以统一语义，图负责适合图表达的关系，明细继续留在数据库，文档继续留在知识库，通过 DataEngine 关联与查询。

## 5.2 图权限必须覆盖六种对象

建议至少考虑：

**对象存在、属性、关系、路径、聚合结果、推导事实。**

只做节点和字段权限是不够的。

例如：

```text
A 公司 → 控制 → B 公司 → 投资 → C 项目
```

用户可能有权看 A 和 C，却无权知道 B，以及这条关系链。

如果系统把 B 隐藏，然后直接告诉用户“A 与 C 存在投资关联”，仍然可能泄露了受限事实。

因此，你需要区分：

**用户可见的图遍历**：只在其获准使用的对象与关系范围内执行。

**经批准的受限计算**：允许可信服务利用隐藏关系计算特定结果，但输出必须经过专门的披露规则。

后者不能由模型自行决定，也不能因为最终没有展示中间节点，就当作没有泄露。

## 5.3 本体读取权限不等于后续输出权限

这一点已经有非常直接的现有产品证据。

Palantir 的对象和属性安全政策文档明确指出，这些政策控制读取，但**不会自动延伸到下游输出或导出**；需要配合标记或分类控制来传播约束。:chatgpt-content-reference{index="5"}

对你的架构来说，含义是：

```text
图中有权限
≠ 可以发给外部模型
≠ 可以加入共享摘要
≠ 可以发送给其他员工
≠ 可以写入长期记忆
```

**出域、共享、保存和执行，应当是分别受控的使用动作。**

## 5.4 不向模型开放“任意遍历整个图”

建议暴露受约束的图能力，而不是一个无限制的 Cypher／SPARQL 接口。

例如，模型可以请求：

```text
分析某类供应中断对获准产线的影响；
只使用当前有效关系；
返回受影响对象的获准表示；
附带证据引用和覆盖范围。
```

本地再编译成具体图查询与必要的数据查询。

关键控制包括：

**起点范围、允许关系类型、时间范围、路径范围、返回属性、聚合粒度、结果规模、计算预算。**

但这些限制不能为了安全而悄悄改变答案语义。假如正确分析必须遍历四层依赖，而政策只允许两层，结果就应明确是“受限范围内的分析”，不能当作完整影响评估。

同样：

> **没有检索到某个关系，不等于该关系不存在。**

可能是权限不足、图未覆盖、数据过期，或者来源本身有缺失。

## 5.5 允许云端分析的，应当是“任务相关、获准披露的子图”

这比把全图匿名化后上传更合理。

例如任务是：

> 判断某类零件中断对产线的影响。

可以在本地构建只包含必要依赖的子图，去除无关客户、价格和合同信息；再根据政策决定，是否允许把其中的关系与拓扑交给云端。

但要注意：

**“最小子图”不等于“安全子图”。**

一条边就可能是核心秘密；一个很小的子图也可能唯一识别一家企业。

因此，子图裁剪解决的是最小化，政策解决的是是否允许披露。这两项不能合并成一个模型自报的“安全分”。

## 5.6 GraphRAG 需要从建库阶段就纳入治理

Microsoft GraphRAG 的索引流程会生成实体、关系、社区及社区报告，并对部分文本和报告生成向量；全局查询会使用这些社区报告组织答案。:chatgpt-content-reference{index="6"}

由此可以推导出一个重要的工程风险：

> 即使查询时过滤了原始文档，一份早先融合了受限资料的共享社区摘要，仍然可能带出那些资料的信息。

所以我建议：

**抽取、Embedding、社区摘要和查询都纳入同一数据边界。**

共享摘要要么在明确的安全域内构建，要么保守继承来源约束；无法证明某段摘要可以向某个受众披露时，就不能只因为它是“模型生成的摘要”而放行。

权限变更或源文档删除后，相关摘要、索引和缓存也要能够失效或重建。

为了避免成本失控，不必给每个用户构建一份独立图；可以优先按稳定的权限域和业务用途组织，但不能牺牲隔离语义。

## 5.7 推导事实需要自己的证据与身份

例如：

```text
供应商风险升高
＋某零件单一来源
＋库存覆盖不足
→ 某产线存在较高停线风险
```

最后这个结论不是原始数据库里直接存在的字段，而是分析产生的新事实。

我建议记录：

**来源证据、使用的规则或模型、时间范围、数据版本、推导过程引用、假设和适用范围。**

尤其要区分：

**系统记录的事实、从文档抽取的主张、模型提出的假设、经验证的分析结论。**

不能因为某句话被写进图，就自动升级成企业事实。

---

# 六、统一后的架构：不是新建一个巨型“隐私中台”

我的建议是：**保留你已确定的三方边界，增加跨组件一致的数据使用约束。**

## 6.1 三个核心系统各负责什么

| 系统 | 继续承担的职责 | 这次需要增强的内容 |
|---|---|---|
| **Agent DataEngine** | 企业语义、数据与知识接入、上下文构建 | 权限感知查询、任务子图、指标绑定、证据与来源约束 |
| **KodaX Runtime** | 唯一 Agent 执行内核、工具、状态、Artifact、验证与恢复 | 分步执行、可信本地计算、信息流跟踪、受控反馈 |
| **KodaX Fabric** | 身份、政策、预算、产能、路由、计量与 Outcome 经济性 | 数据出域条件、允许执行环境、累计披露约束、执行路径经济性 |

MemPrivacy 类组件放在其中，继续处理适合它的敏感片段识别和别名变换。

**它不负责定义业务指标，不负责判断图查询是否完整，也不负责证明优化方案正确。**

## 6.2 两种“编译”不要混淆

你原来已经有 Fabric 的 **Run Compiler**，DataEngine 也有 **Context Compiler**。建议沿用，不再增加第二套 Run 协议。

**DataEngine 的 Context Compiler**回答：

> 对这项任务，应该从哪些数据与知识中形成什么上下文？哪些保持本地？哪些能够变换后提供？

**Fabric 的 Run Compiler**回答：

> 根据这些约束，哪些模型与执行环境可以使用？需要什么授权、预算和产能？允许哪些后续步骤？

**任务执行图和实际工具循环仍由 Runtime 管理。** Fabric 消费这些约束、授权和计量，不变成第二个业务编排器。

## 6.3 从一个请求，编译出两类内容

建议把每次任务形成的内容分成：

**可向模型披露的上下文。**  
可能是代码契约、获准子图、聚合数据、业务规则摘要。

**保留在可信环境中的执行引用。**  
可能是真实数据集、完整仓库、数值参数、内部证据、凭据和敏感结果。

引用本身不是权限。模型知道某个 `artifact_ref`，不意味着就能读取其全部内容。

可以在已有 Compiled Run、Run Grant 和 Artifact 元数据上扩展以下约束：

| 约束 | 用途 |
|---|---|
| 业务目的与代表主体 | 说明这次使用数据的合法业务范围 |
| 数据／本体／代码版本 | 绑定可复现的输入基础 |
| 允许的数据能力 | 读取、聚合、遍历、计算或修改的范围 |
| 允许的模型和执行环境 | 限制原始内容与变换内容的去向 |
| 可披露内容范围 | 字段、关系、结果类型及粒度 |
| 输出接收方 | 区分模型、用户、工具和业务系统 |
| 有效期与重检条件 | 防止权限、状态或数据变化后继续执行 |
| 验证要求 | 规定结果必须通过哪些业务与安全检查 |

这些是内部约束，不是要让业务应用每次手填几十个字段。

## 6.4 形成自适应但受控的推理闭环

不必要求第一轮就决定所有披露。

更合理的是：

```text
提供最小可用上下文
→ 强模型提出分析或补充信息请求
→ 本地判断新增信息是否必要且获准
→ 选择披露、本地计算或切换可信路径
→ 验证反馈
→ 继续任务
```

这样既能避免一开始上传整个仓库或整张图，也不至于因为上下文过少，把强模型变成只能输出通用建议的工具。

但新增披露必须受累计策略限制。**模型“为了完成任务需要更多信息”只能是请求理由，不能直接成为授权。**

---

# 七、还必须增加一层：信息流控制，而不是只做输入输出扫描

## 7.1 派生内容默认继承限制

建议采用一个保守原则：

> **从受限数据派生的结果，默认仍受限；只有经过明确批准的转换，才允许降低披露限制。**

例如，机密代码产生的摘要、客户明细产生的表格、图分析产生的风险结论，都不能自动变成普通文本。

否则会出现：

```text
原文禁止外发
→ 本地模型总结原文
→ 摘要没有敏感词
→ 摘要被放行
```

这很可能只是把泄露形式从逐字复制变成语义转述。

## 7.2 “本地沙箱执行”不自动意味着“结果可以出域”

假设云端生成一段 Python，本地允许它读取经营数据。即使禁止网络访问，它仍然可以把原始数据变成字符串、数字或错误信息，通过正常工具返回带出去。

因此，需要区分：

**代码能不能执行**和**执行结果能不能披露**。

对固定、经过验证的查询与计算能力，可以定义较清晰的输出规则；对任意生成代码，不能只凭“输出是一个 JSON”“只返回十行”就认为安全。

我的建议是：

**常见任务优先采用受约束的能力和算子；确需任意代码时，输出保守继承输入限制，经过单独的释放决策。**

## 7.3 保密等级和内容可信度是两个维度

一篇公开网页可能不敏感，但其中的指令不应获得工具权限。

一份内部报告可能高度敏感，但它的正文同样不应自动成为系统指令。

因此，要分别处理：

**保密性：内容可以流向哪里。**

**可信性：内容能否影响控制流程和授权。**

CaMeL 的研究提供了相关参考：它把控制流与数据流显式区分，并通过能力与工具调用时的政策执行，限制不可信内容造成的越权数据流。这是可借鉴的系统思路，但不能据此宣称任意企业 Agent 已获得普遍安全证明。:chatgpt-content-reference{index="7"}

## 7.4 累计披露记录不能只按单次 Run 管

同一用户可能通过多个任务、多个 Agent、多个模型逐步获取信息。

因此，需要在治理事件中记录：

**什么数据域、什么受众、什么用途下，已经披露过哪些类型的信息。**

这不要求再造第五本经济账，可以与现有审计和 Run 记录关联。

只有采用了正式的差分隐私机制，才应使用具有数学含义的隐私预算。不能把“调用了多少次”“输出了多少行”随意包装成 \(\epsilon\)。

例如 SmartNoise SQL 会累计查询带来的隐私成本，并支持跨多个查询读取器跟踪预算；这说明真正的隐私预算需要配套机制与组合计算，不只是网关里一个数字字段。:chatgpt-content-reference{index="8"}

---

# 八、这是否意味着必须本地部署一个最强大模型？

**不意味着，但也不能指望一个 4B 隐私检测模型承担全部本地智能。**

要把本地工作分开看。

| 本地工作 | 是否必然依赖强大模型 |
|---|---|
| SQL 执行、指标计算、权限检查 | 通常可以由确定性组件完成 |
| 图遍历、关联计算、约束求解 | 主要依赖对应引擎或算法 |
| 代码编译、测试、静态分析 | 主要依赖开发工具链 |
| 复杂私有代码理解 | 可能需要较强本地模型或人工参与 |
| 私有文档的开放式综合推理 | 可能需要较强可信模型 |
| 语义脱敏和必要信息判断 | 可由模型辅助，但不能独立承担硬授权 |

也就是说，你可以通过工具与语义层，把很多工作从“需要最强本地 LLM”转成“需要可靠的本地计算能力”。

但当任务必须理解不能披露的核心语义时，还是要有足够能力的可信推理路径。

机密计算是其中一种可研究的部署方式：它通过可信执行环境和远程证明来保护运行中的数据，并改变服务运营方等参与者的访问边界。:chatgpt-content-reference{index="9"}

不过：

**机密计算不是在任意闭源 API 前面加一个代理，就能让那个模型在你的安全环境里运行。** 必须确认目标模型和完整推理链路真的支持相应部署与证明；普通企业合同、零保留条款、私有网络和可信执行环境，也不是同一种保护。

因此，Fabric 应管理的是**明确的执行环境与信任属性**，不能只有“公有云／私有云”两个标签。

---

# 九、建议怎样落地，避免架构一下子变得过重

## 9.1 先建设共同底座，再做四种任务适配

共同底座只解决几件事：

**上下文与来源约束、出域政策、受控执行引用、输出释放、累计披露记录。**

然后分别建设代码、数据问答、图分析和优化能力，不要求它们使用完全相同的转换方式。

| 任务适配 | 核心交付 | 最重要的验收 |
|---|---|---|
| 代码 | 任务代码上下文、契约、合成样本、本地补丁与测试闭环 | 原始代码上通过测试，修改范围正确，日志和反馈不越界 |
| 数据问答 | 指标语义、受限查询、本地精确计算、结果视图 | 与金标准结果一致，口径正确，查询与输出权限正确 |
| 图分析 | 权限感知查询、任务子图、证据和覆盖说明 | 不通过隐藏路径泄露，结果不把权限缺失当成事实不存在 |
| 业务优化 | 参数绑定、求解、仿真、执行审批 | 满足真实约束，正确报告可行性与最优性，执行状态可核验 |

这里不需要先构建覆盖整个企业的完美本体。可以围绕明确业务对象，逐步补齐语义与能力。

## 9.2 我建议用两个端到端场景验证架构

**一个代码闭环**：真实企业仓库中的限定范围缺陷修复，验证最小上下文、补丁落地、测试反馈和信息流。

**一个业务闭环**：例如“供应延迟影响分析 → 订单／产线影响查询 → 候选调整方案 → 本地优化与验证”，把本体、图、知识证据、业务数据和优化串起来。

这两个闭环能验证你真正关心的问题：

> **不把原始资料整体交给外部模型，是否仍然能完成足够复杂、可验收的任务？**

单独展示“姓名被替换、答案又恢复了”，不能回答这个问题。

## 9.3 ROI 也要按任务类型升级

继续保留你的：

\[
\text{Cost per Accepted Outcome}
\neq
\text{Cost per Token}
\]

但不同任务的 Accepted Outcome 不同。

**代码任务**：不是生成补丁，而是补丁在真实仓库中通过约定验证并被接受。

**数据问答**：不是生成解释，而是数据、口径、权限和证据正确。

**图分析**：不是找到一条看似合理的路径，而是关系有效、覆盖范围明确、结论有依据。

**优化任务**：不是生成“节约 15%”的文字，而是方案满足真实约束；预测收益、仿真收益和实际收益分别记录。

总成本需要包含上下文编译、本地查询、图计算、求解、测试、外部模型、人工复核与失败重做。

**安全约束先决定哪些路径可行，Fabric 再在可行路径中比较经济性。** 不应把一次禁止披露简单折算成金额，然后用更低的 Token 价格抵消。

---

# 最终判断

**你现在需要的，确实已经不是“MemPrivacy＋Token Hub”，而是一套能够按任务控制数据使用、跨域推理和执行结果的架构。**

其中最关键的三项改进是：

**让 DataEngine 把企业数据、代码与知识编译成任务需要的语义上下文和可调用能力；让 Runtime 在可信边界里完成计算、验证与动作执行；让 Fabric 对每一步的数据披露、执行环境、预算与成果经济性实施约束。**

MemPrivacy 仍然有位置，但位置会更清楚：

> **它是处理部分敏感内容的算法组件，而不是整套企业 AI 架构的安全与智能核心。**

真正有价值的能力是：**既不要求企业把所有秘密交给模型，也不因为保护秘密就把模型限制成只能写通用文案，而是让复杂任务在明确的信息边界内，被拆解、执行、验证并完成。**

---

如果你愿意，我可以：

- :chatgpt-content-reference{index="11"}
- :chatgpt-content-reference{index="12"}
- :chatgpt-content-reference{index="13"}