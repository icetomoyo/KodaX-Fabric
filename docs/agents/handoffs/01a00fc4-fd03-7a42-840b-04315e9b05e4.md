# Handoff · 2026-08-17 · install upstream mattpocock skills

Session: `01a00fc4-fd03-7a42-840b-04315e9b05e4`

## 1. Topic

- Asked how to use `npx skills@latest add mattpocock/skills`
- Found `~/.claude/skills/` is a Chinese wrapper, not upstream
- Installed 35 upstream skills into `~/.grok/skills/`; left the wrapper in place

## 2. Two skill trees

| Path | What | Same name in Grok |
|------|------|-------------------|
| `~/.grok/skills/<name>/` | mattpocock **upstream** | **wins** |
| `~/.claude/skills/<name>/` | Chinese wrapper | shadowed |

- Wrapper-only, still usable: `feature-manager` / `issue-manager` / `which-skill` / `smart-release` / `human-test-guide` / `writing-great-skills`
- Upstream entries: `ask-matt` / `grill-with-docs` / `setup-matt-pocock-skills` / `codebase-design` / `triage` / `wayfinder` / `prototype`

## 3. Official chain

```
/setup-matt-pocock-skills → /ask-matt → /grill-with-docs → /to-spec → /to-tickets → /implement
```

Do not mix with the wrapper chain (`/which-skill` → feature-manager).

## 4. Unfinished (as of that session)

- [ ] Repo had not run `/setup-matt-pocock-skills`

## 5. Do first next

1. Official chain: `/setup-matt-pocock-skills` first
2. Skill questions only: `/ask-matt`

## 6. Suggested skills

- `setup-matt-pocock-skills`
- `ask-matt`
- `grill-with-docs`
