# Token 账单跟团队 Key 走，不跟项目走

PRD 把 Virtual Key 绑死项目做成本归因。实际里员工会跨多个项目、多个团队，一把 Key 看不出「这次调用属于哪个项目」，按项目发 Key 也不现实。账单粒度定为团队：员工在每个团队有一把 Key，用量记入该 Key 所属团队。用 A 团队的 Key 去做 B 团队的工作，当作使用问题，系统不拦截。超级管理员挂在默认企业上可以接受。
