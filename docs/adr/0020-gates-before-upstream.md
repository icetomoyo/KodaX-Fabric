# 打上游之前先限流再查预算

若按 Attempt 扣 RPM，failover 会把自己打进 429。若先打上游再 402/429，钱已经花了还拒绝。

限流按入口 HTTP 计 1 次。进上游之前先查限流再查预算：超 RPM → 429，已超硬预算 → 402，两者都不打上游。仍 append 一条 Request（token/cost 0）。回来之后用各 Attempt 成本之和做事后扣减。半开探测也是这笔真实入口，照常计数、照常入账。
