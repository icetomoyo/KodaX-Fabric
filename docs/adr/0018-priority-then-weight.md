# 先优先级，再加权；失败的 Channel 本请求不抽回

同优先级一上来就加权随机，failover 第二跳可能抽回刚失败的那条。没有「只当备路」的表达时，只能靠把权重调到接近 0 来假装备用。

优先级从高到低。同优先级内按权重抽；failover 从同一优先级剩余 Channel 再加权，抽空再降级。刚失败的那条本次入口请求不再抽回。权重 0 = 只当备路，平时不抽，仅在更高优先级全不可用后出现。
