# KodaX-Fabric

## Handoff

Session handoffs live in `docs/agents/handoffs/`.

When the user says "read handoff", "读留言", names the last N notes, or a session ID: read `docs/agents/handoffs/INDEX.md` first, then those files. Do not load handoff bodies until asked.

Write a handoff via `~/.grok/rules/00-handoff.md`. This file is the stable entry — do not overwrite it.

## Agent skills

Issue tracker / triage / domain docs: see `CLAUDE.md`. Details in `docs/agents/`.
