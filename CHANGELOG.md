# Changelog

<!-- last-sync: 42ea5d2 -->

## [Unreleased]

### Removed

- 批量登记席位（`POST /api/admin/channel-seats/bulk`）与批量挂 KEY（`POST /api/admin/channel-seats/bulk-keys`）正式下线，前后端代码与词表解析库一并清理；席位仍可单个登记、编辑、回收，历史操作审计记录不受影响
- 内置敏感词库文件「新思想启蒙」「补充词库」；迁移和种子不再自动灌入词表，词表由管理员自行添加或导入
- 超管侧栏「企业钉钉」及查看钉钉部门树的后台页和接口（`/admin/enterprise-dingtalk`）。钉钉部门 ID 同步脚本和环境变量仍保留

### Changed

- 调用日志与用户分析的积分改为结算同口径：请求审计落库 `started_at` 与结算积分 `request_credits`（迁移 0049），展示优先读结算值、按请求起止区间计算峰谷折扣（跨 14:00/18:00 边界的请求正确显示「跨高峰」），失败请求显示 0 不再虚增积分；升级前的旧日志无落库值，仍按结束时刻估算
- 管理员「测试」渠道 KEY 不再清除或缩短转发路径写入的冷却：测试成功仅在冷却已到期时解除，测试失败只延长不缩短（与 relay 同语义）；用户分析当日明细超 5000 条时积分标注「估算」并按最近 5000 条计算
- 用户分析单日排名改读 usage_counters_daily 预聚合表（与调用审计同事务维护），一次索引扫描替代对审计大表的当天全量 GROUP BY；迁移时从历史审计幂等回填计数行，历史日期的排名结果保持不变；姓名筛选升级为全公司远程搜人，排名 50 名之外的员工也能直接查看当日用量
- 敏感词扫描热路径收敛：词表归一化与 Aho-Corasick 匹配器随 5 秒缓存预计算，匹配成本只随文本长度线性增长、与词数无关（1 万词 × 25.6 万字符约 9ms）；超长字符串不再整段跳过，改为保留首尾各 5 万字符参与扫描；整个请求扫描文本上限 25.6 万字符（超限保留首尾窗口）——超长字段不再是检测盲区，单请求扫描成本有界
- 敏感词拦截必须叠在检测上：检测关闭则不扫、不记、不拦；只开检测则命中写入检测记录并放行；开拦截须同时开检测，命中写入拦截记录并拦住请求。关检测会关掉拦截；系统设置在检测关闭时禁用拦截开关
- 系统设置将敏感词拆成「检测」和「拦截」：检测只记录异常行为，拦截才会挡住员工请求
- 「异常行为记录」拆成「敏感词检测记录」和「敏感词拦截记录」
- 超管侧栏新增「系统设置」；敏感词「启用拦截」从词表页挪到系统设置
- 超管侧栏把用量分析 / 用户分析收进「数据分析」，上游渠道 / KEYS看板 / 调度画布 / 模型列表收进「上游」，调用日志 / 报错日志收进「日志」
- 敏感词管理支持 txt / md / Excel / Word / PDF 批量导入（一行一词）；列表按命中次数排序，每页 10 条
- 渠道 KEY 健康检查「当前异常」改为展示上游原文（如 `[1310] 已达到 7 天使用上限…`），不再用 Hub 改写的冷却说明
- 渠道 KEY 连通性测试改为只走 OpenAI Chat 发一条真实推理（`ping`）；1308/1309/1310 等额度/套餐 429 会按报文冷却时间写入凭证。KEYS看板工具栏提供「测试全部」
- 429 配额调度收尾：报文没带恢复时刻时回退到该 Key 此前学到的窗口重置相位并回写凭证 meta（learnedCapReset，按窗口周期前滚）；本地账本远低于限额时记录站外消耗证据（externalDrain）；配额评估计入在途请求；同优先级按剩余额度选 Key；套餐到期 / 失效类报文并入长冷却
- 渠道 Key 闲置解绑改为绑定满 2 小时且近 2 小时没有调用即回到待绑定池（不再等 5 小时、也不再看 5 小时积分桶）；只在进程启动和每分钟定时扫，转发请求时不再顺带释放
- 标准档渠道 Key 改为按企业共享，一把 Key 最多绑 5 个标准员工；部门只记消耗，不再作为绑定范围。重度仍独占

### Added

- 超管侧栏「敏感词检测」含「敏感词管理」和「异常行为记录」。员工命中敏感词时网关伪装成智谱 1301（HTTP 400，官方敏感内容文案），不提示具体词；命中请求写入异常行为记录（员工、命中词、摘要、请求预览）
- 超管侧栏「临时渠道」下增加 KEYS看板（`/admin/keys-board`）：整页一块泳道看板，列为等候 / 使用 / 5 小时冷却 / 7 天冷却 / 限流 / 停用。Key 用双圈表示额度，点击打开详情；可按姓名搜索提交人或当前连接的人
- 普通上游 429 限流冷却从 60 秒改为 5 分钟（`RELAY_COOLDOWN_SECONDS` 默认 300）
- 调度画布筛选增加「只看已绑定渠道 Key」，默认打开，隐藏未挂渠道 Key 的断开节点

- Token Bot 空态快捷问题改为高频接入：WorkBuddy / ZCode / Claude Code / Codex 怎么对接，以及协议怎么选
- Token Bot / 接入教程补充 WorkBuddy：创建 Key 必须选 OpenAI Chat Completion；自定义模型建议 `glm-5.3-flash` 并勾选「图片输入」，回答里带创建 Key 和添加模型截图
- 一名员工可以同时加入多个部门（仍只属于一家企业）。创建 API Key 时若属于多个部门必须选择绑定部门，只有一个部门则默认显示且不可改。用量记入该 Key 绑定的部门。
- 按集团组织架构图补齐部门树，并把已注册员工挪到对应部门（含一人多部门）
- Token Bot：员工说出自己的部门后可直接加入；同名部门再确认企业。多部门的人仍可让管理员邀请
- 调度画布筛选改为企业 / 部门 / 子部门级联，选到哪一层就只展示该层及其下级的调度

### Fixed

- 智谱错误目录 1308 / 1310 按 coding-plan 线上原文对齐（5 小时 / 7 天打满，带超额按量付费提示）；文档 1317 仍保留为「7 天且无法超额按量」变体。冷却识别同时认这些业务码，Responses 没有数字码时仍靠「使用上限」中文
- 智谱「使用上限」类 429（5 小时 / 7 天 / 每周 / 每月上限）原先识别不出、只按普通限流冷 60 秒，员工在站外直用渠道 Key 打爆额度后会每分钟重试轰炸：现在识别为配额耗尽，优先按上游返回的恢复时刻冷却；报文带了已过期的时刻则按普通 429 短冷却（不前滚窗口）；报文没有时刻时回退本地 5 小时 / 周窗口重置；欠费/余额不足仍走 30 分钟冷却
- 席位页左侧渠道列表与渠道页使用同一套「公司/模型」展示名（官方智谱线路为 GLM（国内版）/（国际版）），并同样展示渠道标签，避免出现「智谱 / 智谱/GLM」这类不一致
- Token Bot 改用 AI Elements Vue 的 Conversation / Message / PromptInput，助手回复按 Markdown 渲染
- 调度画布不再用 Vue Flow 双向绑定把内部量尺寸写回整图，避免每次数节点都重算导致卡顿

### Added

- 超管可在渠道页删除渠道；该渠道下的席位、渠道 KEY 和绑定该渠道的个人 API Key 一并销毁，历史调用日志仍会保留
- 新增渠道供应商可选智谱或海致集团。智谱线路为国内 / 国际；海致集团走自部署开源模型，不选手线路，需填上游地址
- 超管侧栏「上游」拆成渠道、席位、渠道 KEY。新增/编辑渠道须填写席位数量；席位页左渠道右列表，按「已登记 / 总数」展示；登记可加标签，同一员工同一渠道可有多个席位。有席位的员工才在个人中心提交渠道 KEY；回收席位会销毁对应渠道 KEY
- Token Bot 能解答 ZCode 自定义供应商接 Token Hub 后贴图无效：添加 `glm-5.3-flash` 时必须勾选输入类型「图片」，回答里带供应商页和添加模型页截图
- 站内 Token Bot（接入/排障，可查看自己的 Key 与调用）
- 渠道 Key 绑定满 5 小时且近 5 小时 Token、积分都是 0 时自动解绑，回到待绑定池。进程启动即扫，之后每分钟扫一次；有人请求时也会先释放闲置 Key
- 企业管理员可打开调度画布；点击渠道 Key 节点查看绑定与用量，并释放回资源列表。下次请求会重新从资源池挂 Key
- 每次中继请求把完整上下文（请求体、调度轨迹、响应或拼好的 SSE）写成 gzip JSON，按日目录落在服务器本地盘，不进 Postgres；API Key 与渠道 secret 打码。暂不自动清理
- 超管「调用日志」按员工搜索（远程搜人）；列表展示员工、模型、Tokens、积分（含折扣）、状态、时间，操作含详情与下载；详情展示错误、调度轨迹与请求/响应，全文 JSON 仍可下载（部署前的旧日志没有文件）
- 「报错日志」按员工搜索（远程搜人）；列表每页 10 条，展示员工、模型、错误信息、时间；详情展示错误元数据，不含请求体 / 响应 / 上下文 JSON
- 超管「企业管理」改为企业 / 部门 / 员工卡片，选中后可直接编辑、停用、邀请、重置密码等全部操作
- 超管后台「Key 绑定」：在上游渠道和模型列表之间用关系图画企业 → 部门 → 员工 → 虚拟 Key → 智谱 Key；按近 7 日日均消耗把员工标为闲置 / 标准 / 重度（无调用为闲置，不占渠道 Key；注册保护期内默认重度独占）
- Key 绑定图按中继 in-flight 点亮正在干活的虚拟 Key / 智谱 Key：绑定边上青/琥珀粒子表示请求往、token 回，短请求结束后保留约 4 秒余晖
- Key 绑定页按企业拆成独立画布，未绑定 / 冷却 / 停用的 Key 单独放在底部公共池
- 编辑团队时可更换所属部门（同企业内；默认团队不能挪）
- 调度画布改为整页画布：筛选只选企业并展示该企业全部调度；右下角「资源」打开抽屉列出未绑定 Key
- 调度画布组织链改为企业 → 部门 → 命名团队 → 员工；挂在部门上的人不再画出默认团队节点
- 调度画布用部门/团队接口补全组织链，避免旧绑定图接口缺部门字段时企业后面直接接团队
- 调度画布按用量分档连渠道 Key：闲置不连，标准部门共享，重度独占；已取消的企业共享边不再画出。自建渠道仍走共享池
- 标准用户改为按部门共享一把渠道 Key；重度改为近 7 日日均 Token ≥ 3000 万（不再用单日峰值 5000 万）
- 编辑员工调整团队时，下拉只列出其所在部门下的团队
- 关闭注册用户申请合作企业：个人工作台不再展示申请入口，接口拒绝申请；超管侧不再展示审核通过。企业改由超管直接创建，注册用户等待团队邀请

### Fixed

- 工作台「今日 Tokens」按记账日（Asia/Shanghai）汇总团队消耗，与下方 Top 团队同一数据源，不再用 Postgres `current_date` / `created_at::date` 对不上；紧凑数字固定两位小数，避免 7.80 亿显示成 7 亿

### Changed

- 接入教程页顶突出 Base URL，并提示不懂就问右下角 Token Bot；未入团队可先告知企业/团队再问该找谁邀请
- 接入教程第一步：刚注册的人要把手机号交给领导，请对方邀请进团队；没进团队不要建 Key
- 接入教程第二步：先对照 KodaX / KodaX Space / Claude Code / Cursor / Codex / CC Switch 等产品，建 Key 时选对三种协议之一
- 接入教程第三步：到「API Key」按产品创建 Key，协议按对照表选，明文只显示一次
- 接入教程第四步：客户端 Base URL 统一填当前站点 /ai，并对照 Claude Code / Cursor / KodaX / Codex / CC Switch 的填写位置
- 接入教程第五步：到「模型列表」复制模型名做第一次调用，用「我的调用」确认是否成功
- 员工工作台只保留调用数字（今日/本月请求与 Tokens）和快捷入口，去掉用量卡片、接入说明、Key/团队/企业 KPI
- 去掉工单系统：员工提交 / 自查和管理端查询入口、接口与 `tickets` 表一并删除，以后有需要再加
- 新增角色「部门管理员」：企业管理员任命；范围是其所在团队的部门；可建本部门团队、管本部门成员，不能建部门、不能碰上游 KEY
- 管理后台编制入口统一成卡片：超管「企业管理」企业 → 部门 → 团队 → 员工；企业管理员少企业列；部门管理员少部门列；团队管理员只留员工列表。侧栏不再单独放部门/团队/成员/Key 菜单

- 当前组织链改为企业 → 部门 → 团队 → 员工。每个企业有默认部门，每个部门有默认团队：人可以直接挂在部门上（如人力资源部）；需要再拆时才建命名团队（如销售部的华北 / 华南）。部门在没有命名团队、也没有员工时可删除；团队在没有员工时可删除。`teams` / `team_admin` 不改名。用量仍跟团队走，标准用户按部门共享渠道 Key。一名员工只属于一个团队。项目入口、项目表和按项目记账全部去掉
- 用量分档只保留闲置 / 标准 / 重度：闲置不绑渠道 Key，标准按部门共享一把渠道 Key，重度独占。取消轻度（企业共享）档；再有请求时闲置按标准档挂上部门共享 Key
- 去掉人民币成本计算；管理端「模型单价」改为「模型列表」，按渠道展示可用模型与智谱积分消耗系数
- 去掉企业套餐、团队每月额度、个人每日上限；转发不再按这些门槛拦截。智谱 Key 的 5 小时 / 周积分限额保留
- 智谱渠道模型列表只展示当前生效的 `glm-5.3`（文本）和 `glm-5.3-flash`（多模态）；Key 测试带回的历史模型名归并到这两条
- 智谱转发白名单仅允许 `glm-5.3` / `glm-5.3-flash`，其它模型名直接 403 `model not allowed`
- 超管侧栏「Key 绑定」更名为「调度画布」
- 上游 Key 可打可选标签，仅在卡片上展示，不参与调度
- 调度画布按已落库的用量分档画绑定边，不再用未写入的「下一步」分档；否则新用户第一次小流量调用后，独占智谱 Key 会从虚拟 Key 后面消失
- 调度策略重构为分档绑定（重度独占 / 标准部门共享），渠道 Key 支持 5 小时与周积分额度（按智谱积分计量；内置 GLM 官方默认系数；高峰工作日 14:00–18:00 乘数 1、非高峰 0.5），耗尽自动冷却换绑
- 绑定分档：注册起 7×24 小时保护期一律重度独占；期满后按近 7 日日均消耗直接定档，日均 3000 万 Token 及以上为重度，0 调用为闲置（不占渠道 Key）。换档时挂上新范围的 Key，旧范围没人用了才释放
- 新增用量分档「闲置」：保护期满且近 7 日 TokenHub 调用量为 0 时断开虚拟 Key 与渠道 Key，释放渠道 Key；再有请求时按标准档、部门共享重新挂上
- 自定义自建渠道（千问）不走用量分档独占，该渠道下有虚拟 Key 的人共用渠道 Key
- Key 绑定图的共享边只连当前分档会真正用到该范围的虚拟 Key
- 智谱积分：输入按请求开始时刻判高峰；输出按工作日 14:00–18:00（UTC+8）重叠分钟加权，跨平时/高峰不再整单一档
- 智谱 Key 主动冷却：5 小时积分达到 85%、周积分达到 95% 即换绑，不必等到上游 429
- 智谱 Key 周积分窗口改为团队套餐统一相位（上游 18:49 UTC+8 重置，本地从 19:00 切窗，每 7 天），不再按上海周一 00:00
- Key 绑定图把「冷却中」拆成 5 小时冷却与周冷却；限流短冷却仍归在 5 小时列，节点标明恢复时间
- 工程与 npm 包主体名称统一为 **KodaX-Fabric**（`kodax-fabric` / `@kodax-fabric/*`）；Token Hub 作为核心模块名保留

---

## [0.1.0] - 2026-08-11

Fabric · **Token Hub** 公司内可大范围公测收口版（FEATURE_001）。

### Added

- 公测章程与配置基线（渠道、账号、配额 5 亿/人/日、对接人、安全）
- 发布 / 备份恢复 runbook，以及值班故障排查一页纸
- E10 回归清单与实跑记录（含 Claude Code / Cursor 双端验证）
- 员工接入教程强化：双协议对照、Cursor 专节、「一客户端一把 Key」
- 功能与问题追踪：`FEATURE_LIST`、`KNOWN_ISSUES`、收尾说明 `v0.1.0-closeout.md`

### Fixed

- `channel-overview` 默认单测在无 `.env` 时因配置加载失败的问题

### Documentation

- 部署 README 与公测/运维文档互链
- 产品叙事统一为 KodaX Fabric 核心模块 Token Hub

### Notes

- 工程版本号与 monorepo `package.json` 对齐为 **0.1.0**
- 生产 Docker 镜像 tag 仍可能带 `0.0.5-…` 构建后缀，以主机 `compose.yaml` 为准；换镜像时请单独发版镜像
- 已知后续优化：日 Token 触顶时客户端 Retry 文案不友好（KNOWN_ISSUES 001）

---

## [0.0.5] - 2026-08-07

### Added

- 基础工单（员工提交/自查，管理员查询）
- 注册审核、部署与接入相关能力
